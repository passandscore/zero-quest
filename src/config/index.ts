export const CONFIG = {
  TEST_MODE: false,  // Set to false for production
  WINNING_PERCENTAGE: 100,
  TEST_WIN_PERCENTAGE: 100,
  INITIAL_LOADING_MS: 2000,
  TYPING_SPEED_MS: 50,
  NEW_ENTRY_DELAY_MS: 10, // Animation duration in seconds for new table entries
} as const; 